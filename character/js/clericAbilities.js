
function clericAbilities (level)
{
    let message = "";
    
    if(level >= 9)
        {
            message = "May establish or build a stronghold and attract followers to the stronghold.";
        }
    
    return message;
}